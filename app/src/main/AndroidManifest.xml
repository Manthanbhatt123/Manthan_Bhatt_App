<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!-- For Android 10+ scoped storage handling is usually needed, but generic read is good -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CapermintPractical"
        android:usesCleartextTraffic="true">

        <service
            android:name=".services.RecorderService"
            android:exported="false"
            android:foregroundServiceType="microphone">
            <intent-filter>
                <action android:name="org.fossify.voicerecorder.action.GET_RECORDER_INFO" />
                <action android:name="org.fossify.voicerecorder.action.STOP_AMPLITUDE_UPDATE" />
                <action android:name="org.fossify.voicerecorder.action.TOGGLE_PAUSE" />
            </intent-filter>
        </service>

        <activity
            android:name=".presentation.DynamicNanoHttpD.DynamicNanoHttpD"
            android:exported="false" />
        <activity
            android:name=".presentation.nanohttpd.NanoHttpDActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>